# Codex Çalışma Planı — Cadde Redesign **F15 → F23** (2026-08-02)

> **Kim için:** bu planı yürütecek ajan (Codex). Tek başına yeterlidir; ek soru sormadan
> F15'ten başlanabilir. Bağlam gerekirse: [frontend-devir-notu-2026-08-02.md](./frontend-devir-notu-2026-08-02.md)
> (biten F1–F14, kararlar defteri) · [redesign-calisma-haritasi.md](./redesign-calisma-haritasi.md) (madde→dosya haritası).
> Pano: https://corteqs.net/admin/workshop/cadde
>
> **Durum:** F1–F16 bitti ve `main`'de (son F16 commit `ee2fdff`). Kalan 7 batch: F17–F23.
> Repo temiz, tek dal `main`. **F2 (footer) kullanıcının şeridi — `src/components/Footer.tsx`'e dokunma.**

---

## 0. Oyun kuralları (her batch için, istisnasız)

1. **1 batch = 1 commit.** `git add -- <dosyalar>` — **pathspec'siz `git add .` YASAK**
   (kullanıcı aynı repoda paralel çalışıyor; pathspec'siz commit onun dosyalarını süpürür).
2. **Kapılar** (hepsi yeşil olmadan commit yok):
   ```bash
   npx vitest run src/components/cadde/ src/pages/cadde/ src/lib/cadde-*.test.ts
   npx tsc --noEmit -p tsconfig.app.json     # ⚠️ "-p" ŞART, aşağıya bak
   npx eslint <değişen dosyalar>
   npm run verify:text
   ```
   ⚠️ **Bare `npx tsc --noEmit` bu repoda SIFIR dosya denetler** (kök `tsconfig.json` →
   `"files": []` + project references). Referans ölçüm: proje genelinde **109 hata** var ve
   hepsi eski borç (bayat `supabase/types.ts`, eksik `@/lib/mapEntities` · `html-to-image` ·
   `@/lib/whatsappLandings`, ProfilePage `string | boolean`). **Kabul ölçütü: sayı 109'u
   GEÇMEYECEK ve senin dosyalarında hata olmayacak.** Kendi dosyalarını şöyle süz:
   ```bash
   npx tsc --noEmit -p tsconfig.app.json | grep -E "cadde|Cadde"
   ```
3. **Migration akışı** (canlı DB'ye yazan tek doğru sıra):
   ```bash
   # 1) dosyayı supabase/migrations/<version>_<slug>.sql olarak yaz  (version > 20260802130000)
   # 2) KURU KOŞU: begin; <sql> rollback;  ile doğrula
   # 3) uygula:  psql "$PS" -f supabase/migrations/<dosya>
   # 4) history: insert into supabase_migrations.schema_migrations(version,name) values ('<version>','<slug>');
   # 5) dosyayı supabase/migrations/applied/ altına taşı
   ```
   Bağlantı: `PS="host=aws-1-eu-west-2.pooler.supabase.com port=5432 dbname=postgres user=postgres.injprdrsklkxgnaiixzh sslmode=require"`,
   şifre `.env.local` → `SUPABASE_DB_PASSWORD`. **Bash aracında `dangerouslyDisableSandbox: true` şart.**
   Türkçe içerikli SQL'i **daima dosyadan** (`-f`) gönder — komut satırından geçen Türkçe bozulur.
4. **CLAUDE.md Cadde kuralları bağlayıcı:** mutasyon yalnız security-definer RPC ·
   SQL↔TS ayna sözleşmesi iki tarafı + testiyle birlikte değişir · yeni RPC hata kodu →
   `src/lib/cadde-rules.ts:101` `CADDE_RPC_ERROR_MESSAGES` Türkçe haritasına eklenir ·
   ürün limiti/flag'i koda değil **`cadde_settings`**'e yazılır.
5. **Commit mesajı:** konu satırı + gövde (neden) + trailer'lar:
   `Constraint:` `Rejected:` `Confidence:` `Scope-risk:` `Directive:` `Not-tested:`.
6. Batch bitince: **panoda işaretlenecek madde numaraları** + kalan iş tek paragraf raporlanır.

---

## 1. Zemin gerçekleri — bugün canlı DB ve koddan **doğrulandı**

| Gerçek | Değer / yer | Plan açısından anlamı |
|---|---|---|
| Canlı max migration | **`20260802130000`** | Yeni sürümler bundan büyük olacak |
| Tepki CHECK | `cadde_post_reactions_reaction_type_check` → `ARRAY['like','support','idea']` | F15 bunu genişletir |
| Tepki verisi | `like=3 · support=2 · idea=2` (toplam 7) | `idea` göçü **2 satır** — risk sıfır |
| Tepki RPC | `toggle_cadde_reaction_v1(p_post_id uuid, p_reaction_type text)` SECDEF | İmza korunur, gövde genişler |
| Yorum RPC | `create_cadde_comment_v1(p_post_id uuid, p_body text)` SECDEF | F17'de dokunulmaz |
| **Feed RPC** | **`list_cadde_feed_v1(p_filters jsonb, p_cursor jsonb, p_limit integer)`** | ⚠️ filtreler **jsonb** → yeni filtre/eşik için **imza değişmez** |
| **Post RPC** | `create_cadde_post_v1(p_post_type, p_title, p_body, p_country, p_city, p_is_bridge, p_need_category, p_interests text[], p_cafe_id, p_diaspora_key, p_media jsonb, p_mentions jsonb)` — **12 parametre** | ⚠️ Eski plandaki "sabit 6 parametre" bilgisi **YANLIŞ**. İmzayı daima canlıdan (`pg_get_functiondef`) kopyala |
| `cadde_posts` sayaç kolonu | **YOK** (share/comment/reaction sayacı yok) | F19 ve F21 denormalize sayaç ekler |
| `commentCount` nereden geliyor | `src/lib/cadde-api.ts:273` → `postComments.length`, yorumlar `:206`'da **hepsi birden** çekiliyor (`.in("post_id", postIds)`) | F16: eager çekimi kaldırırsan **sayaç bozulur** → önce `head:true` count |
| Tepki UI | Popover: `src/pages/cadde/CaddePage.tsx:685-725` · set: `:63-67` · handler `:202` | F15'in tek JSX bölgesi |
| Yorum girişi | Düz `<Textarea>`: `src/pages/cadde/CaddePage.tsx:783-789` (MentionTextarea **değil**) | F17 Enter mantığı buraya |
| Emoji / paylaşım paketi | `package.json`'da **yok** | F20 bağımlılık kararı gerektirir |
| Sıralama aynası | `src/lib/cadde-ranking.ts:23` `CADDE_RANK_WEIGHTS` (paylaşım ağırlığı yok) | F19/F21 aynayı günceller |
| `cadde_settings` | 18 anahtar var; **`cadde.global.*` ve `cadde.share.*` YOK** | F19/F21 yeni anahtar ekler |
| Ev işi | `supabase/migrations/` altında 3 dosya `applied/`'e taşınmamış (`20260730190000`, `20260802120000`, `20260802130000`) — **üçü de canlıda kayıtlı** | F15 commit'ine ekle (sadece `git mv`) |

**Tepki seti kararı (kullanıcı, kesin):** negatifsiz 5'li →
`like`=Beğendim · `love`=Kalp · `haha`=Gülme · `support`=Destek · `unsure`=Emin olamadım.

---

## 2. Batch'ler

### F15 · m19 + m20 — tepki seti (BİRLEŞİK, migration önce) [M]

**Neden birleşik:** ikisi aynı JSX bloğunu (`CaddePage.tsx:685-725`) değiştirir; ayrı commit'lerde
ikincisi birincisini ezer.

**Önce oku:** `CaddePage.tsx:63-67` (`REACTION_META`), `:200-210` (handler), `:679-725` (Popover),
`src/lib/cadde-types.ts:6` (`CaddeReactionType`), `279` (`reactionCounts`), `93`.

**Migration** (`20260802140000_cadde_reaction_set_v2.sql`) — tek dosyada, bu sırayla:
1. CHECK'i **geçici olarak** 6 değere genişlet: `like, support, idea, love, haha, unsure`.
2. `update cadde_post_reactions set reaction_type='unsure' where reaction_type='idea';` (2 satır)
   — gerekçe: "Fikir" negatif değil ama karşılığı olmayan tek eski değer; kullanıcı önerisi bu.
3. CHECK'i son 5 değere daralt: `like, love, haha, support, unsure`.
4. `toggle_cadde_reaction_v1` gövdesini canlı `pg_get_functiondef` çıktısından kopyalayıp
   içindeki whitelist'i güncelle (imza + grant'ler **aynı kalır**).
5. `cadde_settings`'te `cadde.reaction.minute_limit` zaten var — dokunma.

**UI:**
- `REACTION_META` 5 elemana çıkar (Türkçe etiketler yukarıdaki gibi; ikon: `ThumbsUp`, `Heart`,
  `Laugh`, `Sparkles`, `HelpCircle` — hepsi lucide'da var).
- **Popover kalkar**: 5 tepki doğrudan aksiyon satırında açık, her birinde sayı.
  Dar ekranda `flex-wrap` + `gap-1.5`, dokunma alanı ≥40px.
- `CaddeReactionType` union'ı 5 değere güncelle; `reactionCounts` Record'u derlenmeye devam etsin.

**Ayna/test:** `src/lib/cadde-rules.test.ts` veya yeni `cadde-reactions.test.ts` içinde
"TS union ↔ SQL CHECK aynı 5 değer" testi (CHECK listesini sabit dizi olarak yazıp karşılaştır).
`CaddePage.test.tsx`'te "5 tepki doğrudan görünür, popover yok" testi.

✅ **Bitti sayılır:** kartta 5 emoji açık · eski `idea` tepkileri kayıp değil (`unsure` oldu) ·
ayna testi yeşil · canlıda CHECK 5 değer.

---

### F16 · m21 — yorum sayısı + "devamını yükle" [S/M] — ✅ BİTTİ (`ee2fdff`)

**Kritik sıra:** `commentCount` bugün eager çekilen yorumlardan geliyor (`cadde-api.ts:273`).
**Önce** sayacı bağımsızlaştır, **sonra** eager çekimi kaldır — tersi sayacı sıfırlar.

1. `cadde-api.ts`: `listCaddeFeed` içindeki `fetchComments` çağrısını **yalnız sayı** için
   `select("id", { count: "exact", head: true })` kalıbına çevir (post başına tek toplu sorgu:
   `.in("post_id", postIds)` + group yerine, sayfa başına tek sorgu ile `post_id` sayımı).
   Feed RPC'ye **`comment_count` EKLEMEK YASAK** — o değişiklik F21 zincirinin işi.
2. Yorumlar artık **panel açılınca** çekilir: yeni `listCaddePostComments(postId, limit, cursor)`
   (ilk 5 + "devamını yükle"). React Query key: `caddeQueryKeys` içine ekle.
3. `CaddePage.tsx:739` sayaç metni aynı kalır; `:778-781` boş-durum metni korunur.

✅ Feed açılışında yorum gövdeleri çekilmiyor · sayaç bağımsız · tıkla-aç + devamını yükle
çalışıyor. Not: feed RPC'ye `comment_count` eklenmedi; sayım `cadde_post_comments.post_id`
üzerinden tek hafif sorguyla yapıldı, F21 zinciri korunuyor.

---

### F17 · m22 + m24 — yorum etkileşimi [S]

1. `CaddePage.tsx:783-789` `<Textarea>`'ya `onKeyDown`: `Enter && !shiftKey` → `preventDefault` +
   gönder (boş/whitespace ise no-op), `Shift+Enter` → satır atlama. Deseni
   `src/components/cadde/MentionTextarea.tsx:85-99`'dan al.
2. Yorum düzenini sıkıştır ("jilet"): satır yüksekliği `min-h-[88px]` → `min-h-[64px]`,
   gönder butonu ikon+metin tek satır, yorum listesi `space-y-2` ve avatar/isim satırı tek satıra.

✅ Enter gönderir, Shift+Enter satır atlar · mobilde panel taşmıyor · mevcut testler yeşil.

---

### F18 · m23 — açık yorum dizisinde auto-refresh [M]

**Önce oku:** `src/lib/cadde-feed-polling.ts` (F1'in `newestCaddeCreatedAt` + adaptif aralık
mantığı) ve `CaddePage.tsx`'teki `countCaddePostsSince` kullanımı.

Feed'in adaptif deseni (60s → 2dk → 5dk, kullanıcı görürken durur) **açık yorum paneline**
uygulanır: yalnız `expandedCommentPostId` olan post için `refetchInterval`, panel kapanınca durur.
Yeni yorum gelince sessiz ekle (scroll pozisyonunu bozma).

✅ Karşı yorum makul gecikmeyle görünüyor · panel kapalıyken hiç sorgu yok · polling bütçesi adaptif.

---

### F19 · m12 — Paylaş butonu + `share_count` [M + migration]

**F21'in ön koşulu** (globale çıkış eşiği paylaşım sayısına dayanıyor).

**Migration** (`20260802150000_cadde_share.sql`):
- `cadde_post_shares(id, post_id, user_id, channel text, created_at)` + RLS (kullanıcı kendi
  kaydını görür; INSERT policy **yok** — RPC üzerinden).
- `cadde_posts.share_count integer not null default 0`.
- `record_cadde_share_v1(p_post_id uuid, p_channel text)` SECDEF: `has_cadde_feature` ban
  kill-switch'inden geçir, `cadde_settings → cadde.share.minute_limit` (varsayılan 20) ile
  hız sınırı, log satırı + `share_count` artırımı. Hata kodları: `cadde_share_rate_limited`,
  `cadde_share_post_not_found` → **`cadde-rules.ts:101` Türkçe haritasına ekle**.
- `insert into cadde_settings(key,value) values ('cadde.share.minute_limit','20'::jsonb) on conflict do nothing;`

**UI:** aksiyon satırına Paylaş: `navigator.share` varsa Web Share API, yoksa "linki kopyala"
(`navigator.clipboard`) + toast. Link `/cadde?post=<id>` (SEO-kilitli rotalara yeni path açma).
Her iki yolda RPC çağrılır, sayaç artar.

**Ayna:** `CADDE_RANK_WEIGHTS`'e paylaşım ağırlığı eklenecekse F21'de yapılır — burada yalnız veri.

✅ Paylaş çalışıyor (iki yol) · sayaç artıyor · hata kodları Türkçe · yeni test dosyası.

---

### F20 · m25 — emoji bankası (lazy) [M]

Kullanıcı kararı: **gerçek picker, lazy-load**. `package.json`'da emoji paketi yok →
`emoji-picker-react` veya `frimousse` gibi tek-paket bir çözüm ekle (küçük olanı seç ve
gerekçeyi commit'e yaz). `React.lazy` + `Suspense` ile **yalnız açılınca** yüklenmeli.

Bağlanacağı iki giriş: yorum `<Textarea>` (F17'den sonra) ve composer
(`src/components/cadde/CaddeComposer.tsx`). Seçilen emoji imleç konumuna eklenir.

✅ Ana bundle büyümesi ~0 (kanıt: `npm run build` öncesi/sonrası ana chunk boyutu commit'te) ·
seçici iki girişte · klavye ile kapanıyor (Esc).

---

### F21 · m8 + m11 — konum default'u + globale çıkış eşiği [L] ⚠️ zincir başı

**Zincir kuralı:** F21 → F22 **ardışık ve tek elde**. İkisi de `list_cadde_feed_v1`/
`create_cadde_post_v1` gövdesine dokunur; paralel açılırsa biri diğerini ezer.
Her migration **canlı `pg_get_functiondef` tam gövdesi + grant kopyası** ile yazılır.

**m8 (UI):** composer konum default'u = kullanıcının **kayıtlı** konumu
(`actorContextQuery.data.country/city`), aktif filtre fallback'i silinir. "Konum ekle" çipi kalır.

**m11 (kural):** serbest "global" paylaşım kapanır; içerik eşiği geçince globale çıkar.
- **Migration** (`20260802160000_cadde_global_threshold.sql`):
  - `cadde_posts.reaction_count` + `comment_count` denormalize kolonları + trigger'lar
    (insert/delete) + mevcut 12 post için backfill.
  - `cadde_settings`: `cadde.global.enabled`=`true`, `cadde.global.min_reactions`=`10`,
    `cadde.global.min_comments`=`5`, `cadde.global.min_shares`=`10` (ürün kararı SQL'den değişir).
  - `list_cadde_feed_v1` gövdesinde global kapsam için eşik filtresi. **İmza değişmez**
    (`p_filters jsonb`) → grant'lere dokunmak gerekmez.
- **"Beğeni" tanımı bu batch'te sabitlenir:** `reaction_count` **tüm pozitif tepkiler**
  (5'li setin tamamı; set negatifsiz olduğu için ayrım gereksiz) → commit mesajına yaz.
- **Ayna:** `src/lib/cadde-ranking.ts` + `cadde-ranking.test.ts` eşik/ağırlık değişimini
  yansıtır (SQL↔TS ayna sözleşmesi).

✅ Serbest global atılamıyor · eşiği geçen içerik globalde · eşikler SQL'den değişiyor ·
ayna testi yeşil.

---

### F22 · m9 + m10 — çoklu hedef (+1 ülke/şehir) [L] ⚠️ zincirin ikinci halkası

**Kanıt:** `create_cadde_post_v1` bugün **12 parametreli** ve SECDEF; grant'ler imzaya bağlı →
parametre eklemek **DROP + CREATE + yeniden GRANT** ister. Alternatif ve **önerilen** yol:
**`create_cadde_post_v2`** (yeni imza, eski v1 dokunulmadan bırakılır; UI v2'ye geçer, v1 bir
sürüm boyunca çalışmaya devam eder → geri dönüş kolay).

Tekil→çoğul model **4 katmanda** birlikte değişir: composer UI · RPC · `cadde_posts` hedef
kolonları (ör. `cadde_post_targets(post_id, country_id, city_id)` yardımcı tablosu — tekil
kolonları bozmadan) · feed eşleşmesi + `cadde-ranking.ts` aynası.

**m10:** çoklu hedef **premium kilidi** `cadde_settings` anahtarı olarak gelir
(`cadde.post.multi_target_requires_premium`=`true`); kilit kontrolü RPC içinde
(`has_cadde_feature`) — UI yalnız bilgilendirir.

✅ +1 hedef eklenebiliyor · premium kilidi ayardan yönetiliyor · **eski postlar bozulmadı**
(v1 hâlâ çalışıyor, feed her iki modeli okuyor).

---

### F23 · m26 + m27 — koyu palet + inceltme (BİRLEŞİK) [M/L] — **EN SON**

Yapısal batch'ler bitmeden yapılırsa iş çöpe gider. Aynı dosya/satırlar → tek görsel geçiş:
4 cadde sayfasındaki inline gradyan hex'leri **tek kaynağa** (CSS değişkeni / Tailwind token)
taşı + buton/boşluk inceltme.

✅ Kontrast **AA** · dokunma alanları **≥40px** · inline hex kalmadı · ekran görüntüsüyle
kullanıcı onayı sonrası commit.

---

## 3. Yasaklar (denetimde kanıtlanmış tuzaklar)

- `src/components/Footer.tsx` — **kullanıcının şeridi.**
- SEO-kilitli rotalara dokunma: `/cadde`, `/cadde/cafe/:cafeId`, `/cadde/carsi[/:itemId]`,
  `/lansman`, `/cadre`, `/founders`, `/commercial/<slug>`, `/19051919`, `/anket`.
- Legacy tablolar (`feed_posts`, `feed_likes`, `cafes`, `cafe_memberships`, `user_follows`)
  write-revoked → **policy/grant yeniden açma yok.**
- `profiles` / `user_profiles` / `admin_users` **yok** — `user_role_assignments` +
  `user_profile_attributes` + `is_admin()` / `is_moderator()` kullan.
- Mevcut migration'ları **silme/yeniden sıralama yok**; yalnız yeni dosya.
- `walast.txt` ve `*.wa-export.txt` **asla commit edilmez** (özel WhatsApp dökümü).
- `search_directory_catalog` gevşetilmez (`/directory`'yi etkiler).
- Bare `npx tsc --noEmit` ile "temiz" raporlama **yok** (sıfır dosya denetler).

## 4. Ev işi (F15 commit'ine eklenecek, 1 dakika)

Üçü de canlıda kayıtlı ama `applied/` altına taşınmamış:
```bash
git mv supabase/migrations/20260730190000_workshop_items.sql supabase/migrations/applied/
git mv supabase/migrations/20260802120000_seed_command_center_wa_summary9.sql supabase/migrations/applied/
git mv supabase/migrations/20260802130000_command_center_close_completed.sql supabase/migrations/applied/
```

## 5. Bitince

Her batch sonunda tek paragraf: **hangi maddeler panoda işaretlenebilir**, hangi doğrulama
koştu (sayılarla), kullanıcıda ne kaldı. Tüm plan bitince
[frontend-devir-notu-2026-08-02.md](./frontend-devir-notu-2026-08-02.md)'nin
"Bir bakışta durum" tablosunu ve biten-batch listesini güncelle.

**Kullanıcıda duran işler (senin kapsamında değil):** Coolify deploy · F2 footer · giriş yapmalı
QA turu · hoş geldin maili anahtarı · pano kutularını işaretleme (bugün **29 madde** hazır).
