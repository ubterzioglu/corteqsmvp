# Retired Edge Function Sources

Downloaded from the production project on 2026-09-21 before retirement.

| Function | Retirement reason |
|---|---|
| `diaspora-search` | No application caller; public directory search is the replacement. |
| `relocation-chat` | No application caller; `relocation-assistant` is the replacement. |
| `whatsapp-bot-lookup` | No application caller; response data is a static mock catalogue. |
| `chat-register` | No application caller; retained only as a recovery artifact. |

These are recovery artifacts, not deployable source. Do not deploy them without a fresh
security and product review.
