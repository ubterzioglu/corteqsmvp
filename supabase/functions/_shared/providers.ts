// Shared entrypoint for assistant model providers. The proven relocation
// implementation remains the single executable source until it is moved here.
export * from "../relocation-assistant/providers.ts";
